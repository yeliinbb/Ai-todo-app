const Loading = () => {
  return (
    <div className="w-full h-full bg-gray-100 flex items-center justify-center">
      <span className="pai-loader text-center"></span>
    </div>
  );
};

export default Loading;
