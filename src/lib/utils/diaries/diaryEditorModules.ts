const modules = {
  toolbar: {
    container: [
      ["image","bold", "italic", "underline"],
      [{ list: "check" }],
      ["strike"],
      [{ color: [] }, { background: [] }],
      [{ align: "", }, { align: "center" }, { align: "right" }],
      ["blockquote"]
    ]
  }
};

export default modules;
